repeat wait() until game
repeat wait() until game:GetService("VirtualUser")
repeat wait() until game.Players.LocalPlayer


--[[

Anti AFK

]]


local VirtualUser=game:service'VirtualUser'
game:service('Players').LocalPlayer.Idled:connect(function()
VirtualUser:CaptureController()
VirtualUser:ClickButton2(Vector2.new())
end)
_G.Credits = {
  "Created by Code002Lover & Ente_sama",
  ["Discord"] = "8ytFhuj"
}
local marketplaceService = game:GetService("MarketplaceService")
local lp = game.Players.LocalPlayer
function getname(id)
  return marketplaceService:GetProductInfo(id, Enum.InfoType.Asset).Name
end
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Mystikfluu/uilib/master/uilib.lua"))()
local window = library:CreateWindow(getname(game.PlaceId))
local main = window:CreateFolder("Main")

function farm(name)
  for i,v in pairs(game.Workspace:GetChildren()) do
    if(string.find(v.Name,name)) then
      if(v:FindFirstChild("HealthGui")) then
        local args = {
            [1] = "Animal Selected",
            [2] = v.CharId.Value
        }
        game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
        local args = {
            [1] = "Animal Hit",
            [2] = v.CharId.Value
        }
        local health = tonumber(string.split(v.HealthGui.Health.Amount.Text,"/")[1])
        repeat
          game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
          wait()
          if(v:FindFirstChild("HealthGui")) then
            health = tonumber(string.split(v.HealthGui.Health.Amount.Text,"/")[1])
          else
            health = 0
          end
        until health == 0
      end
    end
  end
end


main:Box("Farm-Name","string",function(value)
_G.Name = value or "Chicken"
end)
main:Toggle("Farm",function(toggle)
_G.AF = toggle
while wait() and _G.AF do
if(_G.Name) then
farm(_G.Name)
end
end
end)
--[[
main:Toggle("Collect Meat",function(toggle)
_G.CM = toggle
while wait() and _G.CM do
  for i,v in pairs(game.Workspace:GetChildren()) do
    if(string.find(v.Name,"Meat")) then
      if(tonumber(string.split(v.Name,"Meat")[2])) then
        local args = {
            [1] = "Meat Collected",
            [2] = 1,
            [3] = 1,
            [4] = tonumber(string.split(v.Name,"Meat")[2])
        }

        game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
      end
    end
  end
end
end)
main:Toggle("Collect Item",function(toggle)
_G.CM = toggle
while wait() and _G.CM do
  for i,v in pairs(game.Workspace:GetChildren()) do
    if(string.find(v.Name,"Item")) then
      if(tonumber(string.split(v.Name,"Item")[2])) then
        local i = 3
        repeat
        local args = {
            [1] = "Item Collected",
            [2] = i,
            [3] = tonumber(v.ItemGui.Count.Text),
            [4] = tonumber(string.split(v.Name,"Item")[2])
        }
        game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
        i = i + 2
      until i>=10 or v.Parent == nil
      end
    end
  end
end
end)
--]]
main:Toggle("Currency",function(toggle)
  _G.C = toggle
  while wait() and _G.C do
    for i,v in pairs(game.Workspace.Currency:GetChildren()) do
      local args = {
        [1] = "Collect Currency",
        [2] = v.Name
      }
      game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
    end
  end
end)
--[[
main:Button("Sell",function()
  firetouchinterest(game:GetService("Workspace").DropOffMeat.TouchPart,game.Players.LocalPlayer.HumanoidRootPart,0)
  wait()
  firetouchinterest(game:GetService("Workspace").DropOffMeat.TouchPart,game.Players.LocalPlayer.HumanoidRootPart,1)

end)
--]]
